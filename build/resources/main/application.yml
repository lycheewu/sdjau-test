---
# 公共配置文件

#variables:
#  # session过期时间，单位：毫秒
#  session-timeout:
#    # pcweb端：8小时
#    pcweb: 28800000
#    # app端：30天
#    app: 2592000000
#  redis-namespace: "sdjzu:test"

# 系统配置
server:
  port: 9006
  servlet:
    session:
      cookie:
        domain: sdjzu-test.com

# spring自身配置
spring:
  # 如果启动本项目时，没有指定环境类型，则使用如下配置文件
  profiles:
    active: dev
  # session配置
#  session:
#    # 使用redis存储session
#    store-type: redis
#    redis:
#      namespace: ${variables.redis-namespace}
  servlet:
    multipart:
      # 限制上传文件大小
      max-file-size: 5MB
      max-request-size: 10MB

